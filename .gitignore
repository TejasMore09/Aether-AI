venv/
__pycache__/
.env
*.csv

# Platform
platform/.venv/
platform/.env
*.egg-info/
.pytest_cache/
.mypy_cache/
.ruff_cache/

# Web app
platform/web/node_modules/
platform/web/.next/
platform/web/.env.local
platform/web/next-env.d.ts

# Staff console (same shape as web/)
platform/console/node_modules/
platform/console/.next/
platform/console/.env.local
platform/console/next-env.d.ts
platform/console/tsconfig.tsbuildinfo
platform/web/tsconfig.tsbuildinfo

# Google Drive sync scratch. This repository lives inside a synced folder, so
# Drive writes and deletes temp files under it while git is running — which is
# how `git add -A` once failed mid-index.
.tmp.driveupload/
