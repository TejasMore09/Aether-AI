# The console talks only to the main brain, never to the customer-facing
# services. That is the point of it being a separate application: this
# deployment has no configuration that would let it reach a tenant API.
AETHER_MAIN_BRAIN_URL=http://localhost:8300
