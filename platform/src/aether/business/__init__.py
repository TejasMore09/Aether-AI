"""The business as a whole, rather than one function of it.

Everything below this package reasons about a single domain in isolation: a
pack scores its own metrics, the engine decides on that pack's numbers alone.
That is the right shape for the decision itself and the wrong shape for
understanding a company, because the findings that matter most to an owner are
usually the ones that span two functions at once.
"""
