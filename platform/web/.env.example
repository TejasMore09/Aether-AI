# Copy to .env.local. Server-only values — never NEXT_PUBLIC_*, because the
# browser must not know or reach the platform APIs directly (BFF pattern).

AETHER_CONTROL_PLANE_URL=http://localhost:8100
AETHER_AGENT_RUNTIME_URL=http://localhost:8200
